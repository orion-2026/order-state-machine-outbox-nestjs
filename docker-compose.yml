services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: 8080
      DATABASE_PROVIDER: sqlite
      DATABASE_CONNECTION_STRING: /data/order-demo.db
    ports:
      - "8080:8080"
    volumes:
      - nest_data:/data

volumes:
  nest_data:
